module counter (
    input clk,
    input reset,
    output reg [7:0] count
);

always @(posedge clk or posedge reset) begin
    if (reset == 1'b1)
        count <= 8'b00000000;
    else
        count <= count + 1'b1;
end

endmodule
